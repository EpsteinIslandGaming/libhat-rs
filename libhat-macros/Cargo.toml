[package]
name = "libhat-macros"
version = "0.10.0"
edition = "2021"
description = "Compile-time signature parsing for libhat"
license = "MIT"

[lib]
proc-macro = true
path = "src/lib.rs"

[dependencies]
